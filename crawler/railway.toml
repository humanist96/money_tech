[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "uvicorn notebooklm_api:app --host 0.0.0.0 --port ${PORT:-8000}"
healthcheckPath = "/docs"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
