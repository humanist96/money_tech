-- ============================================================
-- 027: 014의 3컬럼 유니크 인덱스 제거 (G33 회귀 차단)
-- ============================================================
-- 014는 (video_id, mentioned_asset_id, prediction_type)에 유일성을 걸었다.
-- prediction_type이 키에 있다는 것이 정확히 헤징을 허용한 원인이었고
-- (선행 계획 §3.1 #9), 021이 (video_id, mentioned_asset_id)로 강화하면서
-- 이 인덱스는 잉여가 됐다 — 그런데 지우지 않았다.
--
-- 남겨둔 대가가 지금 드러났다. G33이 INSERT를
--     ON CONFLICT (video_id, mentioned_asset_id) WHERE NOT is_duplicate
-- 로 바꾸면서 **중재자를 021 하나로 특정**했다. 기존 행이 is_duplicate=TRUE
-- 이면(현재 27,584행, 전체의 79.9%) 021 부분 인덱스에 잡히지 않으므로,
-- 같은 (video, asset, type)이 재유입될 때 014 위반이 ON CONFLICT에 흡수되지
-- 않고 **예외로 터져 크롤 트랜잭션 전체를 중단시킨다**. 전환 전의 bare
-- `DO NOTHING`은 모든 유니크 인덱스를 흡수했으므로 이는 회귀다.
--
-- 021이 살아 있는 행의 유일성을 이미 보장하고, is_duplicate=TRUE 행은
-- 평가에서 제외되므로 그들 사이의 유일성은 지킬 이유가 없다.
-- ============================================================

DROP INDEX IF EXISTS idx_predictions_unique;
